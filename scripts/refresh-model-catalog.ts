/**
 * Placeholder for a future catalog refresh task.
 *
 * The bundled catalog currently lives in `packages/core/src/models/index.ts`.
 * A later implementation can fetch provider metadata, normalize it into
 * ModelInfo records, and write a generated catalog module.
 */
console.log("Model catalog refresh is not implemented yet.");
